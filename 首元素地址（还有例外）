#include<stdio.h>
int  main() {
	int arr[10] = { 0 };
	printf("%p\n", arr);//地址——首元素地址
	printf("%p\n", &arr[0]);//同上
	//& arr &数组名 数组名不是首元素的地址 - 数组名表示整个数组8数组名 取出的是整个数组的地址
	//2sizeof(arr)- sizeof(数组名)数组名表示的整个数组- sizeof(数组名)计算的是整个数组的大小
	return  0;
}
